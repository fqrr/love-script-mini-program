image {
    display: block;
}

.row {
    display: flex;
}

.single-line {
    text-overflow: ellipsis;
    overflow: hidden;
    white-space: nowrap;
}

.double-line {
    display: -webkit-box;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
    overflow: hidden;
}